import React from 'react';
import { PlatformOverviewStats } from './PlatformOverviewStats';

/**
 * This component serves as the main view for the 'Dashboard' link in the Super Admin layout.
 * It renders the comprehensive platform overview stats.
 */
export function GlobalStats() {
  return (
    <PlatformOverviewStats />
  );
}